package callumboyd.validator;

import callumboyd.model.Tap;

public class TapValidator {
    public boolean validateTap(Tap tap) {
        //For this assessment trusting that all entries are valid.
        return true;
    }
}
